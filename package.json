{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.8.2"
  },
  "dependencies": {
    "baklavajs": "^2.2.0",
    "scl-to-frequency": "^1.0.3"
  }
}
